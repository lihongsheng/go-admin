## 说明:
供应商需免费完成指定测试题(详见测试题sheet)，含交互视觉设计题、前端开发题、后端开发题、中间件适配层题及系统设计题，总工作量约4.5人天


### 交互+视觉设计：
#### 任务要求：
   1. 使用Figma设计轻量资源管理完整页面：资源列表页、搜索筛选区、详情抽屉
   2. 列表字段：资源名称、资源类型、状态、更新时间、操作按钮（查看/编辑）
   3. 完整覆盖4类页面状态：加载中、空数据、请求失败、正常数据
   4. 输出统一交互、组件、状态规范，给前端开发落地依据
#### 交付物：
   1. Figma设计稿/静态截图（列表+详情全场景）
   2. 页面加载/空/失败3类状态视觉说明文档
   3. 列表、筛选、弹窗基础组件规范
### 前端开发（必答）：
#### 任务要求：
1. Vue3 + TypeScript 实现资源管理页面，严格遵循Figma设计稿
2. 页面能力：关键词搜索、类型/状态筛选、分页/加载更多
3. 所有数据请求仅导入并调用resourceAdapter提供的上层方法，不直接拼接后端URL
4. 页面独立处理UI状态（loading、空、失败展示），业务数据完全由适配层返回

#### 交付物：
1. 可独立运行前端页面工程
2. 完整可交互功能：搜索、筛选、分页、详情、新增编辑弹窗
3. 前端调用适配层方法极简示例代码

### 后端开发：
#### 任务要求：
1. Go / Python + 数据库 实现资源底层业务接口
2. 接口清单：资源分页列表、资源单条详情、资源创建、资源更新
3. 列表接口支持关键词、类型、状态多条件分页查询
4. 鉴权统一使用Mock Token，无需复杂登录逻辑
5. 输出标准接口返回结构（统一成功/失败格式，给适配层做转换依据）

#### 交付物：
1. 可本地启动后端服务
2. 资源数据库建表模型
3. OpenAPI接口文档 + curl调用示例
4. 本地一键启动说明

### 中间件/适配层(后端):
#### 任务要求：
1. 封装前端访问后端的统一抽象API，对外暴露4个标准方法：
   listResources、getResourceDetail、createResource、updateResource
2. 内部统一处理：请求头注入（Token）、后端错误码统一转前端友好提示、请求拦截
3. 内置双环境切换：Mock模拟数据 / 真实后端接口，切换开关仅在此层维护
4. 承接前后端联调：对接前端页面调用、对接后端真实接口，打通完整链路

#### 交付物：
1. resourceAdapter完整源码
2. 环境切换配置说明
3. 三层全链路联调完整调用示例

#### 系统设计:
#### 任务要求：
1. 包含 Mermaid 架构图，并分别写清 前端-中间件、中间件-后端 的职责边界、调用关系